name: not_doing_firebase
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  connectivity_plus: ^6.0.2
  flutter:
    sdk: flutter
  geolocator: ^11.0.0
  google_maps_flutter: ^2.6.1
  http: ^1.2.1
  sensors_plus: ^5.0.1
  shared_preferences: ^2.2.3


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - Images/
    - assets/rides_and_gardens.json
